# ansible-playbook deploy_local.yaml -i inventories/local/hosts --vault-id ~/.tokens/vault.txt

- hosts: docs_server
  strategy: linear
  roles:
    - copy_to_remote
    - deploy
    - cleanup_remote
